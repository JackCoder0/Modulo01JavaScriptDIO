const nome = 'Jackson';
const idade = 20;

if (idade >= 16) {
    console.log(`${nome} pode votar, já que sua idade é ${idade}.`);
}
else{
    console.log(`${nome} não pode votar, já que sua idade é ${idade}.`)
}