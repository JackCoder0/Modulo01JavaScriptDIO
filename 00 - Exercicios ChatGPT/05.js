const primeiro = "String";
const segundo = "Bool";

if (primeiro === segundo) {
    console.log(`As strings "${primeiro}" e "${segundo}" são idênticas!`)
}
else {
    console.log(`As strings "${primeiro}" e "${segundo}" não são idênticas!`)
}